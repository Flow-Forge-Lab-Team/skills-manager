# Source Layout

The Go implementation follows the standard `cmd/` and `internal/` package
layout. This directory is reserved for future non-Go source artifacts if the
project needs generated assets or embedded resources.
